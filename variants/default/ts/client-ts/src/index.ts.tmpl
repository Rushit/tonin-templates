// Typed gRPC client for the `{{ service_name }}` service.
//
// Generated by `tonin service new`. Hand-editing this file is fine —
// `npm run gen` regenerates the `gen/` directory from `../proto/*.proto`
// but does NOT touch the items below it.
//
// Two transports, one SDK
// =======================
//
// The generated stubs work with both:
//
//   - **Node** via `@grpc/grpc-js` — server-to-server HTTP/2, full gRPC
//     framing. Use {{ ServiceCamel }}Client (the default export).
//
//   - **Browser** via `grpc-web` — HTTP/1.1+JSON, needs an Envoy proxy
//     in front of the gRPC service. Pin `grpc-web` as an explicit
//     dependency in your app (it's an optional peer dep here so the
//     Node bundle doesn't carry it).
//
// Auth + retry + circuit-breaker config and the trace/deadline helpers are
// the shared `tonin-client` primitives — identical to the Rust and Python
// peers — re-exported below so callers import everything from
// `{{ service_name }}-client`.

// Generated stubs (regenerate via `npm run gen`).
export * from "./gen/{{ service_name_snake }}";

// Shared client-side primitives from the `tonin-client` package: the
// byte-for-byte parallel of Rust's `tonin-client` crate and Python's
// `tonin_client`. Re-exported so peers write a single import.
export {
  AuthCtx,
  type AuthCtxInit,
  type AuthCtxWire,
  AuthError,
  Backoff,
  CircuitBreaker,
  GrpcStatus,
  injectDeadline,
  injectTraceparent,
  injectTracestate,
  PrincipalKind,
  type RawToken,
  RetryPolicy,
} from "tonin-client";
